name: Checks

on:
  push:
    branches: [main]
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Syntax-check every script
        run: |
          set -e
          for f in *.js js/*.js; do
            if [ "$f" = "user-backend.js" ]; then
              cp "$f" /tmp/user-backend.mjs   # uses ES module syntax
              node --check /tmp/user-backend.mjs
            else
              node --check "$f"
            fi
          done
      - name: Check every page loads all the scripts it needs
        run: |
          npm install --no-audit --no-fund
          node scripts/check-page-scripts.mjs
      - name: Run tests
        run: node --test
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Compile-check the data pipeline script
        run: python -m py_compile scripts/fetch_conditions.py
