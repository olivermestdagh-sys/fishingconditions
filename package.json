{
  "name": "fishingconditions",
  "private": true,
  "description": "Static site + Cloudflare Worker; this file only declares dev tooling for the checks.",
  "scripts": {
    "test": "node --test",
    "check-pages": "node scripts/check-page-scripts.mjs"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "globals": "^15.0.0"
  }
}
